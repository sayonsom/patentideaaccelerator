# ═══════════════════════════════════════════════════════════════════
# IPRamp — Environment Variables
# ═══════════════════════════════════════════════════════════════════
# Copy to .env.local and fill in your values.

# ─── Database (Prisma + PostgreSQL) ──────────────────────────────
DATABASE_URL="postgresql://user:password@host:5432/ipramp?schema=public"

# ─── NextAuth.js ─────────────────────────────────────────────────
# Generate NEXTAUTH_SECRET with: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=

# ─── AWS Cognito (user pool: IP Ramp) ────────────────────────────
# Create a Cognito user pool named "IP Ramp" with:
#   - Sign-in: email only
#   - Self-service sign-up: enabled
#   - Required attributes: email, name
#   - Auto-verify: email
#   - App client: confidential, generate client secret
#   - Callback URL: http://localhost:3000/api/auth/callback/cognito
#   - Sign-out URL: http://localhost:3000
#   - OAuth scopes: openid, email, profile
#   - OAuth grant: authorization code
#   - Identity providers for app client: COGNITO + Google
#   - Hosted UI: configure Google IdP (Google OAuth client ID/secret)
COGNITO_CLIENT_ID=
COGNITO_CLIENT_SECRET=
COGNITO_ISSUER=https://cognito-idp.us-east-1.amazonaws.com/us-east-1_XXXXXXXXX

# ─── AI (optional — users can also set via Settings page) ────────
ANTHROPIC_API_KEY=
